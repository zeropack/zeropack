{
  "name": "app-preset-coffee",
  "version": "0.3.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "zeropack serve",
    "serve": "zeropack serve",
    "build": "zeropack build",
    "test": "true"
  },
  "dependencies": {
    "react": "^16.3.0",
    "react-dom": "^16.3.0"
  },
  "devDependencies": {
    "zeropack-core": "^0.3.0",
    "zeropack-preset-coffee": "^0.3.0"
  }
}
